# 视频处理模块编码计划

## 目标

实现 PRD v1.0 定义的离线课堂录屏 PPT 换页检测模块，输入常见格式视频，输出换页时间表、代表截图、候选帧审计文件和无有效 PPT 区间。

## 技术方案

1. **视频元数据与场景粗筛**
   - 使用 `ffprobe` 获取视频时长。
   - 使用 FFmpeg `select=gt(scene,X),showinfo` 提取候选换页时间。
2. **候选帧精细比较**
   - 候选点附近按固定帧率采样低分辨率分析帧。
   - 将前后帧切成默认 4×3 网格，每块计算 64 位 pHash。
   - 统计超过汉明距离阈值的块占比及平均汉明距离。
3. **置信度与稳定帧**
   - 变化占比高于高阈值时标记 `high`。
   - 处于低、高阈值之间时标记 `low`，保留说明供人工复核。
   - 低于低阈值时判为遮挡、抖动或局部变化，不创建页面。
   - 在候选点后 0.5-3 秒寻找连续帧差异低于阈值的稳定帧；找不到时选差异最小帧兜底并降为低置信度。
4. **规则后处理**
   - 与最近确认页做全局 pHash 去重。
   - 合并停留时间短于默认 5 秒的瞬时页面。
   - 合并连续不稳定窗口，超过默认 30 秒时输出到 `no_ppt_segments`。
5. **输出与可追溯性**
   - 输出 `result.json` 和 `page_NNN.jpg`。
   - 保留候选点前帧、当前帧、稳定帧以及 `candidates.json`。
   - 所有阈值由 JSON 配置文件控制。
6. **验证**
   - 使用单元测试覆盖 pHash、分块变化、置信度分类、稳定帧选择、短页面合并、去重和无 PPT 区间合并。
   - 提供评估命令，针对人工标注结果计算召回率、误判率和时间戳误差。
   - 使用真实 3-5 段课堂录屏完成最终准确率与性能验收。

## 完成标准

- 命令行可运行，缺少 FFmpeg 时给出明确错误。
- 输出字段满足 PRD 示例。
- 关键参数无硬编码，默认配置可直接复制调整。
- 纯算法测试全部通过。
- README 包含安装、执行、配置、输出和验收方法。
